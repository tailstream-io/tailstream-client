package main

import (
	"testing"
)

// Basic smoke test to ensure main compiles and flags work
func TestMainFlags(t *testing.T) {
	// Just ensure the package compiles correctly
	// Actual CLI testing is done via test-client.sh
}
