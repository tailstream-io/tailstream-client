name: Release

on:
  push:
    tags:
      - 'v*'  # Trigger on version tags like v1.0.0

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.23'

    - name: Run tests
      run: |
        cd client
        go test ./...

    - name: Build binaries
      env:
        VERSION: ${{ github.ref_name }}
      run: ./build.sh

    - name: Create Release
      uses: softprops/action-gh-release@v2
      with:
        files: |
          dist/tailstream-client-linux-amd64
          dist/tailstream-client-linux-arm64
          dist/tailstream-client-darwin-amd64
          dist/tailstream-client-darwin-arm64
          dist/checksums.txt
        generate_release_notes: true
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}


